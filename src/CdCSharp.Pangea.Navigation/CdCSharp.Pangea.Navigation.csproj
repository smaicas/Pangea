<Project Sdk="Microsoft.NET.Sdk">
    <PropertyGroup>
		<TargetFramework>net10.0</TargetFramework>
		<ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
        <LangVersion>12.0</LangVersion>

        <!-- Package Metadata -->
        <PackageId>CdCSharp.Pangea.Navigation</PackageId>
        <Description>Navigation feature for CdCSharp.Pangea - typed navigation requests, view location by convention and a navigation host</Description>
        <PackageTags>avalonia;navigation;mvvm;routing</PackageTags>
    </PropertyGroup>

    <ItemGroup>
        <InternalsVisibleTo Include="CdCSharp.Pangea.Navigation.Tests" />
    </ItemGroup>

    <ItemGroup>
        <PackageReference Include="Avalonia" Version="12.1.1" />
    </ItemGroup>

    <ItemGroup>
        <ProjectReference Include="..\CdCSharp.Pangea.Core\CdCSharp.Pangea.Core.csproj" />
    </ItemGroup>
</Project>
